name: CI

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "Hello, world!"
          
      - name: run python script
        run: |
          pip install -r requirements.txt
          python ci.py
